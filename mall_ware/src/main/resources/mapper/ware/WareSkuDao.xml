<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.gulimall.ware.dao.WareSkuDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.atguigu.gulimall.ware.entity.WareSkuEntity" id="wareSkuMap">
        <result property="id" column="id"/>
        <result property="skuId" column="sku_id"/>
        <result property="wareId" column="ware_id"/>
        <result property="stock" column="stock"/>
        <result property="skuName" column="sku_name"/>
        <result property="stockLocked" column="stock_locked"/>
    </resultMap>
    <insert id="addStock">
        UPDATE `wms_ware_sku` SET stock=stock+#{skuNum} WHERE sku_id=#{skuId} AND ware_id=#{wareId}
    </insert>
    <update id="lockSkuStock">
        update `wms_ware_sku` set stock_locked = stock_locked+1 where sku_id=#{skuId} and ware_id=#{ware} and stock-stock_locked>=#{num}
    </update>
    <update id="unLockStock">
        update wms_ware_sku set stock_locked=stock_locked+#{skuNum} where sku_id=#{skuId} and ware_id=#{wareId} and stock-stock_locked>=#{skuNum}
    </update>
    <select id="getSkuHasStock" resultType="java.lang.Long">
        Select SUM(stock-stock_locked) FROM `wms_ware_sku` WHERE sku_id=#{sku}
    </select>
    <select id="ListWareIdJasStock" resultType="java.lang.Long">
        select ware_id from `wms_ware_sku` where sku_id=#{skuId} AND stock-stock_locked>0
    </select>


</mapper>